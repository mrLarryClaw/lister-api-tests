[project]
name = "lister-api-tests"
version = "1.0.0"
description = "Comprehensive API test suite for Lister.ai"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
]

[tool.pytest.ini_options]
# Not using pytest — this is a standalone test runner